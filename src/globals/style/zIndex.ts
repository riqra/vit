const base = 1000;

const zIndex = {
  mobileStepper: base,
  fab: base + 50,
  appBar: base + 100,
  drawer: base + 200,
  modal: base + 300,
  snackbar: base + 400,
  tooltip: base + 500
}

export default zIndex;
